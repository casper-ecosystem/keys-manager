{
  "name": "contract-as",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "asbuild:optimized": "asc assembly/index.ts -b dist/keys-manager.wasm --validate --optimize --use abort=",
    "asbuild:test-contract": "asc assembly/test-contract.ts -b dist/test-contract.wasm --validate --optimize --use abort=",
    "asbuild": "npm run asbuild:optimized && npm run asbuild:test-contract",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "assemblyscript": "^0.9.1"
  },
  "dependencies": {
    "casper-contract": "^1.2.0"
  }
}
